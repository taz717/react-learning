console.log("test"); // this is pre much just print()... python gang

var myName = "taz" // var is the old way to do variables
console.log(myName);

myName = "taztaz"  // vars still work AND can be changed
console.log(myName);

let myNumber = "12" // let is the new way to do a var that CAN change
console.log(myNumber);

myNumber = "15" // as stated above... it do be changing
console.log(myNumber);

const myYear = "2020"  // consts are vars that don't change, kinda stubborn imo
console.log(myYear);

myYear = "2021" // this is giong to return an error
console.log(myYear);

